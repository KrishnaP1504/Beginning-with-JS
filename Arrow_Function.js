const add = (a, b) => {
    return a + b;
  };
  
  // For single-line functions, it's even shorter!
  // The 'return' is implicit (automatic).
  const subtract = (a, b) => a - b;
  
  console.log(add(5, 90));      // 95
  console.log(subtract(10, 4)); // 6